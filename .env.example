# Connection to the Postgres database shared with n8n.
# These values match the docker-compose in the n8n/ folder.
# Copy this file to .env.local and adjust if you change the credentials.
DATABASE_URL=postgresql://zuk:zuk_prototype_2026@localhost:5434/zuk

# n8n base URL (used to trigger the confirmation send via webhook).
N8N_BASE_URL=http://localhost:5678
